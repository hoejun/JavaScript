array.push() : 맨끝 삽입
array.pop() : 맨끝 제거
array.shift() : 맨앞 제거
------------------------------------------
slice() : 원본 x, 배열, index1부터 index2 앞까지
substring() : 문자열, index1부터 index2 앞까지

splice() : 원본 O, 배열, index1 부터 개수만큼
substr() : 문자열, index1부터 개수만큼
------------------------------------------
str.split('') : ''기준으로 배열로 리턴
Array.join('') :  ''기준으로 문자열로 리턴
------------------------------------------
str.String() : 숫자->문자열
str.toUpperCase() : 소->대문자
str.toLowerCase() : 대->소문
------------------------------------------
중복 제거에 많이 씀...
발견 못 할시 -1
str.indexOf('str') : 인덱스 값 반환
Array.indexOf('array') : 인덱스 값 반환

str.includes('str'): boolean 반환
-------------------------------------------
filter(): true 배열 리턴
-------------------------------------------
sort() : a-b 오름차순, b-a 내림차순
reverse() : 뒤집기
-------------------------------------------
new Array(length).fill(number) : 배열로 length 만큼 number 채우기
-------------------------------------------
Math.max(), Math.min() : 최대, 최소
--------------------------------------------
string.replace(/[^a-z]/g, '') : 문자열에서 소문자가 아닌 문자를 모두 '' 대체
-------------------------------------------
isNaN('') : 문자 검사
------------------------------------------
let hash = new Map() : map 자료형 객체 생성
hash.set(key, value), hash.get(key)
hash.has(key) : key값이 map내에 존재하는 지 반환
has.set('str', value) 또는 has.set('str', has.get('str')+1)으로 하나씩 늘려줌
for(let [key, value] of hash)
