const deepCopy = JSON.parse(JSON.stringify(original)); // 코드가 복잡
const deepCopy = _.cloneDeep(original) // 코드는 간단하나 lodash 성능이 안 좋고, 라이브러리 설치해야함
const deepCopy = structuredClone(original) // js 기본 내장함수로 노드 v17.0.0 버전부터 지원
